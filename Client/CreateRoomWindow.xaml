<Window x:Class="Client.CreateRoomWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Client"
        mc:Ignorable="d"
        Title="Create Room" Height="450" Width="800"
        FontSize="20"
        Style="{StaticResource DarkWindow}">
    <Grid>
        <StackPanel HorizontalAlignment="Stretch" VerticalAlignment="Center" Margin="20" Width="500">

            <Grid Margin="0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Label x:Name="roomNameLabel" Content="Room Name:" Grid.Column="0"/>
                <TextBox x:Name="roomNameInput" Grid.Column="1" Margin="10,0,0,0"/>
            </Grid>

            <Grid Margin="0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Label x:Name="questionTimeLabel" Content="Time For Each Question:" Grid.Column="0"/>
                <TextBox x:Name="questionTimeInput" Grid.Column="1" Margin="10,0,0,0"/>
            </Grid>

            <Grid Margin="0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Label x:Name="maxPlayersLabel"  Content="Max Players:" Grid.Column="0"/>
                <TextBox x:Name="maxPlayerInput" Grid.Column="1" Margin="10,0,0,0"/>
            </Grid>

            <Button x:Name="createButton" Content="Create" Width="120" Margin="0,10" Click="createButtonPressed"/>

        </StackPanel>
    </Grid>
</Window>